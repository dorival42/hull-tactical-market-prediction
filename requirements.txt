# ─── Python 3.12 / 3.13 — all packages have pre-built wheels ────────────────
# runtime pinned via .python-version and runtime.txt

# Core
numpy~=2.2.0              # 2.x required for Python 3.13; loads numpy 1.x pickles fine
pandas~=2.2.0
plotly~=5.20.0
streamlit~=1.44.0
kaggle~=1.6.0

# ML inference — versions match pkl files exactly
scikit-learn~=1.7.0    # saved with 1.7.2
lightgbm~=4.5.0        # saved with 4.5.0
xgboost~=3.1.0         # saved with 3.1.1
catboost~=1.2.0        # Python 3.12/3.13 wheel available (>=1.2.7)
scipy~=1.14.0

# MLflow & Experiment Tracking
mlflow~=2.17.0          # 2.17+ accepts pyarrow>=16 (no upper bound)
dagshub~=0.3.0

# Configuration
pyyaml~=6.0
python-dotenv~=1.0.0
omegaconf~=2.3.0

# Visualization
matplotlib~=3.9.0
seaborn~=0.13.0

# API & Serialization
grpcio~=1.68.0
protobuf~=4.25.0
pyarrow~=18.0.0         # Python 3.12 & 3.13 wheels available

# Utilities
tqdm~=4.66.0
joblib~=1.4.0

# Statistics
statsmodels~=0.14.4
